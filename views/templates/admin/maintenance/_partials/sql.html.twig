{#**
 * PS Legalcompliance
 * Module for PrestaShop E-Commerce Software
 *
 * @author    Markus Engel <info@onlineshop-module.de>
 * @copyright Copyright (c) 2025, Onlineshop-Module.de
 * @license   commercial, see licence.txt
 *#}
<div class="col-xl-6">
  <div class="card">

    <h3 class="card-header">
      <i class="material-icons">inventory</i> {{ 'Database'|trans({}, 'Modules.Pslegalcompliance.Admin') }}
    </h3>

    <div class="card-body">
      <p>{{ 'The SQL schema and all differences to the current database are shown here.'|trans({}, 'Modules.Pslegalcompliance.Admin') }}</p>

      {% if sql.tables is not empty %}
      <table class="sql_list table">
        <thead>
          <tr>
            <th>{{ 'Table Name'|trans({}, 'Modules.Pslegalcompliance.Admin') }}</th>
            <th>{{ 'Status'|trans({}, 'Modules.Pslegalcompliance.Admin') }}</th>
          </tr>
        </thead>
        <tbody>
          {% for table in sql.tables %}
          <tr>
            <td>{{ table.name }}</td>
            <td>
              {% if not table.valid %}
              <span class="badge badge-warning">{{ 'failure!'|trans({}, 'Modules.Pslegalcompliance.Admin') }}</span>
              {% else %}
              <span class="badge badge-success">{{ 'ok'|trans({}, 'Modules.Pslegalcompliance.Admin') }}</span>
              {% endif %}
            </td>
          </tr>
          {% endfor %}
        </tbody>
      </table>
      {% else %}
      <p class="alert alert-info">{{ 'There is no SQL to install for this module'|trans({}, 'Modules.Pslegalcompliance.Admin') }}</p>
      {% endif %}

      {% if sql.differences is not empty %}
      <div class="alert alert-warning">
        <p>{{ 'Differences:'|trans({}, 'Modules.Pslegalcompliance.Admin') }}</p>

        {% for difference in sql.differences %}
          <code>{{ difference }};</code><br>
        {% endfor %}

      </div>
      {% endif %}
    </div>

    <div class="card-footer">
      <div class="d-flex justify-content-end">
        <a class="btn btn-primary" href="{{ path('ps_legalcompliance_maintenance_sql_reset') }}">{{ 'Reset SQL'|trans({}, 'Modules.Pslegalcompliance.Admin') }}</a>
      </div>
    </div>

  </div>
</div>
